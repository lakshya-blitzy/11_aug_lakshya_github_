# Minimal configuration for FrameworkManager ad-hoc testing
framework.name=AutomationFramework
framework.version=1.0.0
selenium.driver.path=/usr/bin/chromedriver
api.base.url=http://localhost:8080

# Optional properties for better testing
testng.thread.count=5
selenium.browser=chrome
selenium.timeout=30
api.timeout=5000